import 'package:flutter/material.dart';

class CircleUIConfig {
  final double? radius;
  final Color? borderColor;
  final Color? fillColor;

  const CircleUIConfig({this.radius, this.borderColor, this.fillColor});
}
